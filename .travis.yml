language: php

php:
  - 5.2
  - 5.3
  - 5.4
  - 5.5

env:
  - OMEKA_BRANCH=master

before_script:
  - ./.travis.setup.sh

before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script: ./.travis.tests.sh

notifications:
  irc: "irc.freenode.org#slab"

